# SAIE backend — Python dependencies
# Docs: docs/24_Brevo_Email_Integration.md, docs/10_Backend_Architecture.md
# Traceability: FR-050, FR-051, NFR-002, NFR-004, NFR-005, NFR-006, NFR-007, NFR-012

# --- Production ---------------------------------------------------------
# M01 — Project Foundation
fastapi>=0.110.0          # HTTP framework (NFR-006)
uvicorn[standard]>=0.27.0 # ASGI server (NFR-005)
pydantic>=2.6.0           # Typed contracts (NFR-006)
pydantic-settings>=2.2.0  # .env overlay (NFR-004)
opentelemetry-api>=1.24.0     # Tracing API (NFR-005)
opentelemetry-sdk>=1.24.0     # Tracing SDK (NFR-005)
python-dotenv>=1.0.0      # .env loading

# Pre-M01 — Brevo email transport (M12 reporting)
requests>=2.31.0

# M02 — Authentication & Authorization (FR-053, FR-054, FR-057, NFR-004)
# RS256 JWT verification via JWKS + the dev issuer used by the test
# suite. cryptography provides the RSA primitives the dev issuer needs
# to mint tokens in CI without an external Keycloak.
pyjwt>=2.8.0              # JWT decode / verify (FR-053)
cryptography>=42.0.0      # RSA key generation for dev issuer (FR-053)

# M03 — Database & Storage (FR-001, FR-008, FR-019, FR-038, FR-043, NFR-004, NFR-006, NFR-007)
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
asyncpg>=0.29

# --- Test-only ----------------------------------------------------------
# M01 — Project Foundation
pytest>=7.0.0
pytest-cov>=4.1.0
httpx>=0.27.0             # Required by FastAPI TestClient

# M03 — Database & Storage (test-only)
pytest-postgresql>=6.0

# M04+ — Type / lint tooling
ruff>=0.4.0
mypy>=1.10.0
